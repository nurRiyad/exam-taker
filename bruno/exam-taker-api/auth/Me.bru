meta {
  name: Me
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Returns the authenticated user for the current bearer token — run Login
  or Signup first to populate {{token}}.
}
