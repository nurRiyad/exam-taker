meta {
  name: Reset Password
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/auth/reset
  body: json
  auth: none
}

body:json {
  {
    "identifier": "teststudent1",
    "code": "",
    "newPassword": "newpassword123",
    "newPasswordConfirmation": "newpassword123"
  }
}

docs {
  Redeems a 6-character code from "Generate Reset Code". Fails if expired
  (1h), already used, or mismatched.
}
