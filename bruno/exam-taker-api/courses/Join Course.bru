meta {
  name: Join Course
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/courses/:id/join
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id:
}

docs {
  Student only. Login-required join flow (ADR-0048) — enrolls the current
  student in the course.
}
