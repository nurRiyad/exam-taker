import { PlaceholderPage } from "@/components/placeholder-page";

export default function AdminSupportPage() {
  return <PlaceholderPage title="Support" description="Read-only support lookup placeholder." />;
}